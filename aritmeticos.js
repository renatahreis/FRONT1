const [a,b,c,d] = [3,5,1,15]

const soma = a + b + c + d   // operacoes aritmeticas sao sempre binárias
const subtracao = d - b
const multiplicacao = a * b
const divisao = d/a
const modulo = a % 2        // resultado é o resto da divisão 


console.log (soma, subtracao, multiplicacao, -divisao, modulo)
