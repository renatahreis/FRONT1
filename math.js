const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2);  //  Math e um objeto que tem uma constante que armazena o PI E tem uma funcao que calcula a potencia (Math.pow) 

console.log(area);
console.log (typeof Math);